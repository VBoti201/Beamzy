services:
  - type: web
    name: beamzy-relay
    runtime: docker
    plan: free
    rootDir: relay
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
